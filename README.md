# optitrack-mocap-python
Drop-in NatNet client that streams 6-DOF rigid body state at full hardware rate. One config file, no ROS, no extras. Includes fixes for stale-pose and velocity-scaling bugs in the upstream SDK.
